/*
  More About Cargo and Crates.io
      Extending Cargo with Custom Commands
*/

pub fn fn_14_05_01_01() {
    println!("-------------------------------------------------- 01");
    {
        /*
          如果 $PATH 下面某个程序的名称为：cargo-something，
          则它相当于是 cargo 的一个 subcommand，可以通过：cargo something 命令来使用，
          就像使用一个内置的 cargo 命令一样（如：cargo install）；
        */
    }
}
